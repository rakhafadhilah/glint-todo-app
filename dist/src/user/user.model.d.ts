export declare class User {
    id: string;
    name: string;
    username: string;
    email: string;
    password: string;
    token: string;
    isActive: boolean;
    role: number;
    constructor(id: string, name: string, username: string, email: string, password: string, token: string, isActive: boolean, role: number);
}
